<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BIKEMATES_ADMIN.Pages.Settings"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource PageBg}">

    <Grid RowDefinitions="Auto,*">
        <VerticalStackLayout Grid.Row="0" BackgroundColor="{StaticResource Navy}" Padding="18,16,18,18" Spacing="4">
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                <VerticalStackLayout Spacing="2">
                    <Label Text="Settings" Style="{StaticResource HeaderTitle}" />
                    <Label Text="Shop preferences and configurations." Style="{StaticResource HeaderSubtitle}" />
                </VerticalStackLayout>
                <Button Grid.Column="1" Text="&lt;" Style="{StaticResource BackButton}" Clicked="Exit_Clicked" VerticalOptions="Center" />
            </Grid>
        </VerticalStackLayout>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="14,12,14,30" Spacing="14">
                <Label Text="SHOP SETTINGS" Style="{StaticResource SectionLabel}" />
                <Frame Style="{StaticResource CardFrame}">
                    <VerticalStackLayout Spacing="12">
                        <Grid ColumnDefinitions="*,Auto">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="Accept service bookings" Style="{StaticResource BodyText}" />
                                <Label Text="Show the shop as available for customer requests." Style="{StaticResource TinyMuted}" />
                            </VerticalStackLayout>
                            <Switch Grid.Column="1" IsToggled="True" />
                        </Grid>
                        <Grid ColumnDefinitions="*,Auto">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="Show services in customer search" Style="{StaticResource BodyText}" />
                                <Label Text="Published services stay visible in customer booking flow." Style="{StaticResource TinyMuted}" />
                            </VerticalStackLayout>
                            <Switch Grid.Column="1" IsToggled="True" />
                        </Grid>
                        <Grid ColumnDefinitions="*,Auto">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="Notify on low stock" Style="{StaticResource BodyText}" />
                                <Label Text="Use product stock levels to trigger inventory alerts." Style="{StaticResource TinyMuted}" />
                            </VerticalStackLayout>
                            <Switch Grid.Column="1" IsToggled="True" />
                        </Grid>
                    </VerticalStackLayout>
                </Frame>
                <Label Text="PAYMENT" Style="{StaticResource SectionLabel}" />
                <Frame Style="{StaticResource CardFrame}">
                    <VerticalStackLayout Spacing="8">
                        <Label Text="PayMongo: Connected" TextColor="{StaticResource Green}" FontAttributes="Bold" />
                        <Label Text="Payment keys and webhooks are controlled by BikeMate backend deployment settings." Style="{StaticResource SmallMuted}" />
                        <Button Text="Open Reports" Style="{StaticResource OutlineButton}" Clicked="Reports_Clicked" />
                    </VerticalStackLayout>
                </Frame>
                <Label Text="SECURITY" Style="{StaticResource SectionLabel}" />
                <Frame Style="{StaticResource CardFrame}">
                    <VerticalStackLayout Spacing="8">
                        <Button Text="Change Password" Style="{StaticResource OutlineButton}" Clicked="ChangePassword_Clicked" />
                        <Button Text="Manage Login Sessions" Style="{StaticResource OutlineButton}" Clicked="Sessions_Clicked" />
                        <Button Text="Sign Out" Style="{StaticResource DangerButton}" Clicked="SignOut_Clicked" />
                    </VerticalStackLayout>
                </Frame>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>

</ContentPage>
