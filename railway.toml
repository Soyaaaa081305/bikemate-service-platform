[[services]]
  name = "api"
  builder = "dockerfile"
  dockerfile_path = "Dockerfile.api"

  [services.build]
    args = { BUILD_CONFIGURATION = "Release" }

  [services.healthcheck]
    path = "/health"

[[services]]
  name = "webadmin"
  builder = "dockerfile"
  dockerfile_path = "Dockerfile.webadmin"

  [services.build]
    args = { BUILD_CONFIGURATION = "Release" }
